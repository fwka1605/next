import {MenuAuthority} from './menu-authority.model';
import {ProcessResult} from './process-result.model';
export class MenuAuthoritiesResult {
    public processResult: ProcessResult;
    public menuAuthorities: MenuAuthority[];
}

